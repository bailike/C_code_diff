# Code对比工具 v1.0

---

## 一、产品概述

**Code对比工具**是一款离线C++代码比对工具，以左侧文件为基准、右侧文件为对比目标，逐行显示差异。单HTML文件实现，双击浏览器即可打开，无需安装任何环境或依赖。

**文件**：`code_diff_tool_v1.0.html`

---

## 二、界面布局

```
+-----------------------------------------------------------------------+
| [导入左文件] [导入右文件] [交换] [重置] | [整体对比] [选择区域]        |
| [执行对比] | [上一差异] [下一差异] 差异计数                            |
| [同步滚动□] [忽略空行/注释□] [忽略空白✓] [忽略大小写✓]                |
| [折叠相同□] [折叠不同□] | [跳转A] [跳转B]               [统一视图]    |
+-----------------------------------------------------------------------+
|  搜索A: [___________] [大小写□] [↑][↓] 共N个 || 搜索B: 同上           |
+-----------------------------------+-----------------------------------+
|  文件A: name | N行                |  文件B: name | M行                |
|  行号  |  代码                    |  行号  |  代码                    |
|    1   |  #include "stdafx.h"     |    1   |  #include "stdafx.h"     |
|    2 >>|  void [main]() {    ←黄色|    2 >>|  int  [test]() {   ←黄色 |
|        |                         |    3   |    printf("x");     ←绿色 |
|    3   |    return 0;       ←红色 |        |                         |
|        | | 相同折叠 — 8行 |       |        | | 相同折叠 — 8行 |       |
|   12   |  }                       |   12   |  }                       |
+-----------------------------------+-----------------------------------+
|  状态栏：就绪 | A 选中: 12-35行 (24行) | 编码: UTF-8                   |
+-----------------------------------------------------------------------+
|  🔍 区域对比结果 (可拖拽上边框调整高度)                                |
|  [关闭]                                                               |
|  行号 | 左侧代码          | 行号 | 右侧代码                            |
+-----------------------------------------------------------------------+
```

---

## 三、默认配置

| 复选框 | 默认 | 说明 |
|--------|------|------|
| 忽略空行/注释 | □ | 不过滤，注释和空行参与对比 |
| 忽略大小写 | ✓ | 统一转为小写后比较 |
| 忽略空白 | ✓ | trim + 合并连续空格后比较 |
| 同步滚动 | □ | 整体对比时自动开启 |
| 折叠相同 | □ | ≥10行连续相同代码折叠 |
| 折叠不同 | □ | ≥10行连续不同代码折叠 |
| 搜索大小写 | □ | 不区分大小写搜索 |

---

## 四、功能清单

### 4.1 文件操作
| 功能 | 操作 | 说明 |
|------|------|------|
| 导入文件 | 按钮 / 拖拽 | 支持 .cpp .h .c .hpp .txt 等 |
| 编码检测 | 自动 | UTF-8 BOM检测 + GBK启发式 + 手动选择 |
| 交换文件 | ⇄ 按钮 | 一键交换左右面板内容 |
| 重置 | 重置按钮 | 恢复所有复选框到默认值、清空选区/搜索/diff、面板回顶 |

### 4.2 对比视图
| 功能 | 操作 | 说明 |
|------|------|------|
| 整体对比 | 按钮 | 自动开启同步滚动、滚到第1行、执行diff |
| 区域对比 | 两步操作 | （1）点击"选择区域"进入选择模式 → （2）点击行号选范围 → （3）Shift+行号扩展 → （4）点击"执行对比" |
| 统一视图 | 按钮（最右侧） | 单栏 +/- 前缀视图，切换回并排 |
| 忽略选项 | 复选框 | 忽略注释空行 / 忽略空白 / 忽略大小写，勾选后自动重新对比 |
| 折叠选项 | 复选框 | 折叠相同 / 折叠不同，≥10行触发。首尾行保留可见，折叠标记可点击toggle |

### 4.3 差异导航
| 功能 | 快捷键 | 说明 |
|------|--------|------|
| 下一差异 | F7 | 蓝色边框高亮当前差异 |
| 上一差异 | Shift+F7 | |
| 差异计数 | — | 显示"差异: (3/25)" |
| 字符级差异 | — | modified行内：左侧红色删除线、右侧绿色高亮 |

### 4.4 搜索
| 功能 | 快捷键 | 说明 |
|------|--------|------|
| 聚焦搜索 | Ctrl+F | 左右面板独立搜索栏 |
| 下一结果 | F3 / Enter | 橙色高亮匹配文本 |
| 上一结果 | Shift+F3 | |
| 大小写开关 | 复选框 | 默认不区分 |

### 4.5 导航
| 功能 | 快捷键 | 说明 |
|------|--------|------|
| 跳转到行 | Ctrl+G / 按钮 | 目标行居中显示，若在折叠区先展开 |
| 同步滚动 | 复选框 | 对比时默认开启，跳转/区域选择时自动关闭 |

### 4.6 其他
| 功能 | 说明 |
|------|------|
| 文本选择复制 | 代码面板可鼠标拖选复制 |
| 虚拟滚动 | 大文件只渲染可视区域±150行缓冲区 |
| 面板宽度调整 | 拖拽中间分隔条 |
| 区域面板高度调整 | 拖拽区域对比面板上边框 |

---

## 五、差异算法

### 5.1 算法总览

以**左侧文件为基准**，5阶段处理：

```
阶段1：精确内容匹配（哈希 + 位置比例窗口）
阶段1.5：近邻位置配对（±30行紧窗口）
阶段2：左序生成差异条目
阶段3：右侧剩余行追加
阶段4：相邻 removed+added 合并为 modified
阶段5：modified行字符级单词差异
```

### 5.2 阶段1 — 精确内容匹配

- 右侧文件建哈希索引 `Map<hash, [行号列表]>`
- 遍历左侧每行，计算期望右侧位置：`expectedB = round(i × lenB / lenA)`
- 在 `expectedB ± maxWindow` 窗口内找**内容完全相同**的候选行
- `maxWindow = max(600, 15% × max(lenA, lenB))`
- 选距离最近且未被占用的行，无单调约束

### 5.3 阶段1.5 — 近邻位置配对

- 对阶段1未匹配的左侧行，在 `expectedB ± 30行` 紧窗口内匹配
- 取第一个未占用的右侧行，强制配对为 modified
- 解决"函数名不同但位置对应"的场景（如 `BindEx` vs `Bind`）

### 5.4 阶段2~3 — 条目生成

- 严格按左侧行顺序遍历
- 匹配行 → `same`（同内容）或 `modified`（异内容）
- 未匹配左侧行 → `removed`
- 未匹配右侧行 → `added`（插入在对应位置）
- `nextB` 指针 `Math.max` 防回退

### 5.5 阶段4~5 — 后处理

- 相邻 `(removed, added)` 合并为 `modified`
- modified 行做单词拆分 → 单词LCS → 标记删除/新增
- 左侧显示删除单词（红色+删除线），右侧显示新增单词（绿色）

### 5.6 差异类型与颜色

| 类型 | 含义 | 左侧 | 右侧 | 背景色 |
|------|------|------|------|--------|
| `same` | 相同 | 代码+行号 | 代码+行号 | 白色 |
| `added` | 仅右侧有 | 空行 | 代码+行号 | 浅绿 `#e6ffe6` |
| `removed` | 仅左侧有 | 代码+行号 | 空行 | 浅红 `#ffe6e6` |
| `modified` | 不同 | 代码（红删除线） | 代码（绿高亮） | 浅黄 `#ffffd0` |

---

## 六、折叠引擎

- **阈值**：≥10行连续相同/不同触发折叠
- **显示**：保留首行和末行可见，中间插入上下折叠标记
- **格式**：`| 相同折叠 — 折叠行数 N |`（折叠态）/ `| 此处可折叠 — 折叠行数 N |`（展开态）
- **交互**：点击标记 toggle 折叠/展开，保留滚动位置
- **状态**：`collapsedFolds` (Set) 追踪当前折叠

---

## 七、颜色方案（浅色主题）

| 元素 | 颜色值 |
|------|--------|
| 页面背景 | `#ffffff` |
| 文字 | `#1a1a1a` |
| 工具栏/搜索栏 | `#f0f0f0` / `#f5f5f5` |
| 边框/分隔线 | `#d0d0d0` |
| 行号列 | `#999` 文字 / `#fafafa` 背景 |
| 新增行 added | `#e6ffe6` 代码 / `#d4f0d4` 行号 |
| 删除行 removed | `#ffe6e6` 代码 / `#f0d4d4` 行号 |
| 修改行 modified | `#ffffd0` 代码 / `#f0f0c0` 行号 |
| 当前差异边框 | `#0066cc` |
| 字符删除 | `#ffb0b0` + 删除线 |
| 字符新增 | `#90ee90` |
| 搜索高亮 | `#ffa500` 橙色 |
| 搜索当前项 | `#ff8800` + 边框 |
| 区域选中行 | `#cce0ff` 代码 / `#b0d0f0` 行号 |
| 折叠-相同 | `#f8f8f8` 背景 / `#888` 文字 |
| 折叠-不同 | `#fffde6` 背景 / `#996600` 文字 |
| 状态栏 | `#0066cc` 背景 / `#fff` 文字 |
| 按钮激活态 | `#0066cc` 背景 / `#fff` 文字 |

---

## 八、键盘快捷键

| 快捷键 | 功能 |
|--------|------|
| `Ctrl+F` | 聚焦当前面板搜索框 |
| `F3` | 下一个搜索结果 |
| `Shift+F3` | 上一个搜索结果 |
| `F7` | 下一个差异 |
| `Shift+F7` | 上一个差异 |
| `F5` | 重新执行整体对比 |
| `Ctrl+G` | 跳转到行（居中显示） |
| `Esc` | 关闭对话框 / 清除搜索 |
| `Ctrl+C` | 复制选中行文本 |

---

## 九、数据流

```
test1.cpp ──→ [编码检测] ──→ linesA[]
                               │
                    ┌──────────┤
                    ↓          ↓
              [原始渲染A]  [过滤引擎]
                    │    (注释/空行/空白/大小写)
                    │          │
test2.cpp ──→ [编码检测] ──→ linesB[]
                               │
                    ┌──────────┤
                    ↓          ↓
              [原始渲染B]  [过滤引擎]
                    │          │
                    ↓          ↓
              [用户选行]   [computeDiff]
                    │      5阶段差异算法
                    │          │
                    │     [displayList]
                    │     (折叠引擎)
                    │          │
                    ↓          ↓
              [区域对比面板] [差异渲染 + 虚拟滚动]
                             /        \
                     [搜索高亮]    [差异导航]
                     [同步滚动]    [跳转行号]
```

---

## 十、v1.0 测试结果

| 测试文件 | test1.cpp (基准) | test2.cpp (对比) |
|----------|-----------------|-----------------|
| 原始行数 | 27,090 | 27,592 |
| 过滤后行数 | 18,471 | 18,170 |
| 差异条目总数 | 19,668 | |
| 完全相同行 (same) | 12,037 (61.2%) | |
| 修改行 (modified) | 4,936 (25.1%) | |
| 仅左侧行 (removed) | 1,498 (8.1%) | |
| 仅右侧行 (added) | 1,197 (6.6%) | |
| 总体匹配率 | 91.9% (A) / 93.4% (B) | |
| 算法耗时 | 62ms | |
| 行号完整性 | ✓ 无缺失/重复 | |
| 左侧行号顺序 | ✓ 严格升序 | |

---

## 十一、文件清单

```
code对比/
├── 文档.txt                        # 原始需求
├── test1.cpp                       # 测试文件A（左侧基准）
├── test2.cpp                       # 测试文件B（右侧对比）
├── code_diff_tool_v1.0.html        # 工具本体（v1.0）
├── 工作计划与逻辑设计.md            # 迭代计划文档
└── Code对比工具_v1.0.md             # 本文档
```
